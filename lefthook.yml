pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm run format
      stage_fixed: true
    lint:
      run: pnpm run check

commit-msg:
  commands:
    commit-msg:
      run: pnpm commitlint --edit $1
